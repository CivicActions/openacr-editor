name: Deploy
permissions:
  contents: write

on:
  pull_request:
  push:
    branches:
      - gh-pages
      - main
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Use Node.js 20
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: export BASE_PATH=/openacr-editor/ && npm run build
      - run: cp dist/index.html dist/404.html
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.6.4
        with:
          BRANCH: gh-pages
          FOLDER: dist
